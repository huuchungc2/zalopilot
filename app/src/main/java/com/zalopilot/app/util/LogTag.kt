package com.zalopilot.app.util

/**
 * Tag có cấu trúc cho log.json — map 1:1 lên UI tab Nhật ký.
 */
enum class LogTag {
    POLL,
    EVENT_HINT,
    SCAN,
    FOUND,
    CLICK,
    SCROLL,
    ERROR,
    STATE,
}
